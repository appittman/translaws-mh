# Anti-Trans Laws and Mental Health

This is one of my dissertation papers, about how anti-trans laws impact mental health. Here's the workflow:

1.  Run `laws.R` and `hps_data.R`
2.  Run `data_merge.R`
3.  Run `did.R`
4.  Run `results.R`
5.  Run `tables.qmd` and `figures.qmd`
